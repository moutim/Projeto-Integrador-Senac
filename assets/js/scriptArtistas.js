const radioButtonsArtists = document.getElementsByName('time-range-artists');
const btn = document.querySelector('.teste-aa');

const setArtists = async () => {
    // const data = await fetchData('v1/me/top');
    console.log(acessToken);
}

btn.addEventListener('click', setArtists);